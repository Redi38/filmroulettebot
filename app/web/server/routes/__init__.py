"""Route modules, one per concern, assembled onto the FastAPI app in
../__init__.py. See that file's docstring for the assembly/ordering
rationale (route registration order matters — see routes/items.py vs.
routes/tracked_series.py)."""
