{
  "name": "filmroulettebot-web-assets",
  "private": true,
  "version": "1.0.0",
  "description": "Build tooling for app/web/static/js. Not a frontend framework project — the app itself stays plain JS with no import/export; esbuild here is used purely to concatenate + minify the existing global-scope scripts into one file.",
  "type": "module",
  "scripts": {
    "build:js": "node scripts/build-js.mjs",
    "watch:js": "node scripts/build-js.mjs --watch",
    "build:css": "node scripts/build-css.mjs",
    "watch:css": "node scripts/build-css.mjs --watch",
    "build": "npm run build:js && npm run build:css",
    "generate:types": "python3 scripts/dump_openapi.py && openapi-typescript scripts/openapi.json -o app/web/static/js/types/api.d.ts",
    "check:js": "tsc --project tsconfig.json"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "openapi-typescript": "^7.13.0",
    "typescript": "^5.9.3"
  }
}
